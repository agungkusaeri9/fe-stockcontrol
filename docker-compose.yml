services:
  cmwi-stock-control:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cmwi-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - stock-control-network

networks:
  stock-control-network:
    driver: bridge 